apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skale.fullname" . }}-test-connection"
  labels:
    {{- include "skale.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skale.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "skale.fullname" . }}
  labels:
    {{- include "skale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skale.selectorLabels" . | nindent 4 }}
